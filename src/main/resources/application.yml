spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true
  datasource:
    url: jdbc:mysql://localhost:3306/WIBdb?serverTimezone=UTC
    username: root
    password: dmsdn0205